import Employee from "./employee";

//  only responsible for calculating salary
class EmployeeSalaryCalculator {
  computeSalary(emp: Employee) {
    return 1000;
  }
}

export default EmployeeSalaryCalculator;
